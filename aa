 DBObject payload = source.getPayload();
        if (payload != null) {
            Object addressObj = payload.get("address");
            if (addressObj instanceof List) {
                List<?> addresses = (List<?>) addressObj;
                if (!addresses.isEmpty() && addresses.get(0) instanceof DBObject) {
                    DBObject firstAddress = (DBObject) addresses.get(0);
                    return (String) firstAddress.get("line");
                }
            }
        }
        return null;
